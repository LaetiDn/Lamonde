/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ({

/***/ "./assets/js/app.js":
/*!**************************!*\
  !*** ./assets/js/app.js ***!
  \**************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

var APP = __webpack_require__(/*! ./utils/config */ "./assets/js/utils/config.js");

var Header = __webpack_require__(/*! ./components/Header */ "./assets/js/components/Header.js");

var Portfolio = __webpack_require__(/*! ./components/Portfolio */ "./assets/js/components/Portfolio.js");

APP.DEBUG = true;
$(document).ready(function () {
  var header = new Header().init();

  if ($(".portfolio")) {
    $(".portfolio").each(function (index) {
      var portfolio = new Portfolio($(".portfolio").eq(index)).init();
    });
  }

  var screenWidth = $(window).width();
  var heroHeight = $('.hero').outerHeight(true); //Force trigger animations when loading page

  $(window).scrollTop($(window).scrollTop() + 1);

  if (screenWidth <= 992) {
    $('body').addClass('width-mobile');
  } else {
    $('body').removeClass('width-mobile');
  }

  function isMobileDevice() {
    return typeof window.orientation !== "undefined" || navigator.userAgent.indexOf('IEMobile') !== -1;
  }

  ;

  if (isMobileDevice()) {
    $('body').addClass('is-mobile');
  }

  $(window).resize(function () {
    var screenWidth = $(window).width();

    if (screenWidth <= 992) {
      $('body').addClass('width-mobile');
    } else {
      $('body').removeClass('width-mobile');
    }
  }); //Pour le page loader?

  var pageLoader = $('.loader');

  if (pageLoader.length) {
    var time = pageLoader.data('time');
    $('body').addClass('body-no-overflow');
    setTimeout(function () {
      $('body').removeClass('body-no-overflow');
      pageLoader.remove();
    }, time);
  } // $('body').on('click', '#open-newsletter', function (e) {
  //     console.log('plop');
  //     $('.sticky-newsletter').toggleClass('show-newsletter');
  // });


  $('#menu-btn').on('click', function () {
    $(this).toggleClass('open');

    if ($('.header').hasClass('open')) {
      setTimeout(function () {
        $('.header').removeClass('open');
      }, 400);
      $('.site-navigation').removeClass('show');
    } else {
      $('.header').addClass('open');
      $('.site-navigation').addClass('show');
    }

    $('html, body').toggleClass('body-no-overflow');
  }); //Ouvre le sous menu quand on click sur un lien parent

  $('.menu-item-has-children').on('click', function () {
    if ($('body').hasClass('width-mobile')) {
      $(this).find('.sub-menu').toggleClass('open-menu');
    }
  });
  /* SOURCE : https://codeable.io/wordpress-accessibility-creating-accessible-dropdown-menus */
  // Focus styles for menus when using keyboard navigation

  $('nav[role="navigation"]').focus(); // Properly update the ARIA states on focus (keyboard) and mouse over events

  $('[role="menubar"]').on('focus.aria  mouseenter.aria', '[aria-haspopup="true"]', function (ev) {
    $(ev.currentTarget).attr('aria-expanded', true);
  }); // Properly update the ARIA states on blur (keyboard) and mouse out events

  $('[role="menubar"]').on('blur.aria  mouseleave.aria', '[aria-haspopup="true"]', function (ev) {
    $(ev.currentTarget).attr('aria-expanded', false);
  });
  var $window = $(window);
  $window.scroll(function () {
    if ($window.scrollTop() >= heroHeight) {
      $('.main-nav-outer').addClass('sticky');
      $('.book-bottom').addClass('book-bottom-sticky');
    } else {
      $('.main-nav-outer').removeClass('sticky');
      $('.book-bottom').removeClass('book-bottom-sticky');
    }
  });
  $('.menu-item-has-children').on('click', function () {
    if ($('body').hasClass('is-mobile')) {
      $(this).find('.sub-menu').toggleClass('open-sub-menu');
    }
  }); //Open and close form reservation

  $("body").on("click", ".btn-close-form", function (e) {
    e.preventDefault();
    $('.form-main-ctn').fadeOut(600);
  });
  $("body").on("click", ".close-lightbox", function (e) {
    e.preventDefault();
    $('.form-main-ctn').fadeOut(600);
  });
  $("body").on("click", ".open-form", function (e) {
    e.preventDefault();
    $('.form-main-ctn').fadeIn(600);
  });
  $("body").on("click", ".carrousel-nav__button", function (e) {
    var index = $(this).data('index');
    $(this).parent().addClass('active').siblings().removeClass('active');
    $(this).parents('.image-ctn').find('.slide0' + index).addClass('active').siblings().removeClass('active');
  });
});

function triggerCounter() {
  document.querySelectorAll('.counter').forEach(function (item) {
    var element = item.getElementsByClassName('counter__things')[0];
    var number = element.dataset.number;
    var speed = element.dataset.speed;
    var count = 1;
    var idInterval = setInterval(function () {
      element.innerHTML = ++count;
      count++;

      if (count >= number) {
        clearInterval(idInterval);
      }
    }, speed);
  });
}

document.querySelectorAll('.event-link').forEach(function (item) {
  item.addEventListener('click', function (event) {
    event.preventDefault();
    var index = item.dataset.index;
    removeClassFromSiblings(item.parentNode, 'active');
    removeClassFromSiblings(item.parentNode, 'selected');
    addClassFromSiblings(item.parentNode, 'no-hover');
    item.parentNode.classList.add('selected');
    setTimeout(function () {
      removeClassFromSiblings(item.parentNode, 'no-hover');
    }, 1000);
    setTimeout(function () {
      item.parentNode.classList.add('active');
    }, 1300); //Il faut remove tous les show-img avant

    var image = document.getElementById('img-' + index);
    removeClassFromSiblings(image, 'active');
    image.classList.add('active');
  });
});

var animation = _toConsumableArray(document.querySelectorAll('.animate'));

var optionsAnimation = {
  rootMargin: '0px',
  threshold: 0.50
};

var callbackAnimation = function callbackAnimation(entries) {
  entries.forEach(function (entry) {
    if (entry.intersectionRatio >= optionsAnimation.threshold) {
      if (entry.target.hasAttribute("data-animation")) {
        var animation = entry.target.dataset.animation;
      }

      if (animation === 'js-counter') {
        triggerCounter();
      } else {
        entry.target.querySelector('.css-animation').classList.add('do-animation');
      }
    }
  });
};

var observerAnimation = new IntersectionObserver(callbackAnimation, optionsAnimation);
animation.forEach(function (animation, index) {
  observerAnimation.observe(animation);
}); //Helpers

function removeClassFromSiblings(elem, classToRemove) {
  var sibling = elem.parentNode.firstChild;

  while (sibling) {
    if (sibling.nodeType === 1 && sibling !== elem) {
      sibling.classList.remove(classToRemove);
    }

    sibling = sibling.nextSibling;
  }
}

;

function addClassFromSiblings(elem, classToRemove) {
  var sibling = elem.parentNode.firstChild;

  while (sibling) {
    if (sibling.nodeType === 1 && sibling !== elem) {
      sibling.classList.add(classToRemove);
    }

    sibling = sibling.nextSibling;
  }
}

;

/***/ }),

/***/ "./assets/js/components/Header.js":
/*!****************************************!*\
  !*** ./assets/js/components/Header.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var Header = function Header() {
  var _this = this;

  this.root = document.querySelector("header.header");
  var prevScrollpos = window.pageYOffset;

  this.init = function () {
    _this.registerEvents();
  };

  this.registerEvents = function () {
    window.addEventListener('scroll', onScroll);
  };

  onScroll = function onScroll() {
    var currentScrollPos = window.pageYOffset;

    if (currentScrollPos > 300) {
      if (prevScrollpos > currentScrollPos) {
        _this.root.classList.remove("retract");
      } else {
        _this.root.classList.add("retract");
      }

      prevScrollpos = currentScrollPos;
    }
  };
};

module.exports = Header;

/***/ }),

/***/ "./assets/js/components/Portfolio.js":
/*!*******************************************!*\
  !*** ./assets/js/components/Portfolio.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

var Portfolio = function Portfolio(el) {
  var _this = this;

  this.root = el;

  this.init = function () {
    _this.root.imagesLoaded(function () {
      var grid = _this.root.find('.item-wrapper').masonry({
        itemSelector: "#" + _this.root[0].id + " .item",
        transitionDuration: 0,
        percentPosition: !0
      });
    });
  };
};

module.exports = Portfolio;

/***/ }),

/***/ "./assets/js/utils/config.js":
/*!***********************************!*\
  !*** ./assets/js/utils/config.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

var APP = {
  DEBUG: true
};
module.exports = APP;

/***/ }),

/***/ "./assets/sass/style.scss":
/*!********************************!*\
  !*** ./assets/sass/style.scss ***!
  \********************************/
/*! no static exports found */
/***/ (function(module, exports) {

// removed by extract-text-webpack-plugin

/***/ }),

/***/ 0:
/*!*********************************************************!*\
  !*** multi ./assets/js/app.js ./assets/sass/style.scss ***!
  \*********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(/*! C:\www\maximus\htdocs\content\themes\maximus\assets\js\app.js */"./assets/js/app.js");
module.exports = __webpack_require__(/*! C:\www\maximus\htdocs\content\themes\maximus\assets\sass\style.scss */"./assets/sass/style.scss");


/***/ })

/******/ });